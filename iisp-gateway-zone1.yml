spring:
  application:
    name: iisp-gateway
  profiles: zone1

ribbon:
  eureka:
    enabled: true

zuul:
  prefix: /api
  routes:
    client:
      path: /property/**
      serviceId: property-service
